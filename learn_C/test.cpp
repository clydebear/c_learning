#include <bits/stdc++.h>

using namespace std;
int testconstuse(const int &a,const int &b)
{

}
int main(int argc, char **argv)
{
  std::vector<int> v;
  for(int i = 0;i<10;i++)
  {
    v.push_back(i);
  }
  testvetcor(v);
  vector<int>::iterator it;
  for(it = v.begin();it != v.end();it++)
  {
    cout<<*it<<" ";
  }
    return 0;
}
